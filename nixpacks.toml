[phases.setup]
aptPkgs = ["libexif-dev"]

[phases.install]
cmds = ["install-php-extensions pcntl exif"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-reqs",
  "npm ci --no-audit --no-fund",
  "npm run build"
]

/* ============================================================
   ASTRAGO MARKET — frontend bootstrap
   axios is intentionally NOT wired: the app uses Livewire + native
   POST forms exclusively, so shipping axios saved ~40KB from the bundle.
   ============================================================ */